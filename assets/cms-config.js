// Textalk CMS-instellingen.
// Deze waarden koppelen de site aan de GitHub-repo, zodat content die je via
// /admin toevoegt automatisch op de pagina's verschijnt (via de GitHub API).
// Valt terug op content/<collectie>/manifest.json als de API onbereikbaar is.
window.TEXTALK_CMS = {
  user: "HqHu229",       // GitHub-gebruikersnaam
  repo: "textalk-site",  // repositorynaam
  branch: "main"
};
